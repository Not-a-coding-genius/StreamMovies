# Google Service Account JSON (for production deployment)
# Copy your entire service account JSON content as one line
GOOGLE_SERVICE_ACCOUNT_JSON=

# Login Credentials (optional - these are the defaults)
LOGIN_USER1=samyuctaa
LOGIN_PASS1=yukshar
LOGIN_USER2=sharada
LOGIN_PASS2=yukshar

# Movie URL (optional)
MOVIE_URL=

# Port (optional - defaults to 10000)
PORT=10000
